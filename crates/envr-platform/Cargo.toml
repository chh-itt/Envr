[package]
name = "envr-platform"
version = "0.1.0"
edition = "2024"
license.workspace = true

[dependencies]
envr-error = { path = "../envr-error" }

[dev-dependencies]
tempfile = { workspace = true }
