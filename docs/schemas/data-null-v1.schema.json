{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/envr-workspace/envr/blob/main/docs/schemas/data-null-v1.schema.json",
  "title": "envr JSON when data is absent (most failures)",
  "description": "Used when `success` is false and there is no structured payload (e.g. `emit_envr_error`, validation missing-args).",
  "type": "null"
}
