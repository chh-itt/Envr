{
  "runtime": "perl",
  "mode": "exec",
  "command": ["perl", "-MDigest::SHA", "-e", "print Digest::SHA::sha256_hex('abc'), chr(10);"],
  "expect": {
    "exit_code": 0,
    "stdout_regex": "^[a-f0-9]{64}\\r?\\n$"
  }
}

