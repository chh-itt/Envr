{
  "runtime": "racket",
  "mode": "exec",
  "command": ["racket", "-e", "(require json) (displayln (jsexpr->string (hash 'a 1 'b 2)))"],
  "expect": {
    "exit_code": 0,
    "stdout_regex": "^\\{\"a\":1,\"b\":2\\}\\r?\\n$"
  }
}

