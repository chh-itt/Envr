{
  "runtime": "deno",
  "mode": "exec",
  "command": ["deno", "eval", "const s=new TextEncoder().encode('abc'); const h=await crypto.subtle.digest('SHA-256',s); console.log(Array.from(new Uint8Array(h)).map(b=>b.toString(16).padStart(2,'0')).join(''));"],
  "expect": {
    "exit_code": 0,
    "stdout_regex": "^[a-f0-9]{64}\\r?\\n$"
  }
}

